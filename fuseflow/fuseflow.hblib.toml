[[Preamble]]
content = '''from dataclasses import dataclass
import datetime
import os
import polars as pl'''

[[Preamble]]
content='''class Dir:
    stage = 1

    def make(name):
        time = datetime.datetime.today().strftime("%Y-%m-%d-%H-%M-%S")
        dir = f"output-{time}/{Dir.stage * 10:03d}-{name}"
        os.makedirs(dir, exist_ok=True)
        Dir.stage += 1
        return dir'''

[[Preamble]]
content='''def carry_over(src_object, dst_object, *, file=None):
    def carry_one(file):
        src = f"{src_object.path}/{file}"
        dst = f"{dst_object.path}/{file}"
        if os.path.islink(src):
            src = os.readlink(src)
        os.symlink(src=src, dst=dst)

    if file is None:
        for file in os.listdir(src_object.path):
            carry_one(file)
    else:
        carry_one(file)'''

[Type.MlirProgram]
params.path = "Str"
info.title = "@intermediate:An MLIR program in your filesystem"
info.description = """This is the program that we use to generate the schedule."""
info.param_titles.path = "Path to the MLIR program"
info.code = '''@dataclass
class MlirProgram: 
    """An MLIR program in your filesystem

    This is the program that we use to generate the schedule."""
    path: str 
    """Path to the MLIR program"""'''

[Prop.P_MlirProgram]
params.path = "Str"
info.title = "An MLIR program in your filesystem"
info.description = """This is the program that we use to generate the schedule."""
info.param_titles.path = "Path to the MLIR program"

[Function.F_MlirProgram]
params = {}
ret = "MlirProgram"
condition = [
    "P_MlirProgram { path = ret.path }"
]
info.title = "An MLIR program in your filesystem"

[Type.VectorizationPass]
params = {}
info.title = "VectorizationPass"
info.description = """The goal of this step is to produce the schedule for the parallelization
pass in the FuseFlow compiler"""
info.code = '''@dataclass
class VectorizationPass:
    """VectorizationPass

    The goal of this step is to produce the schedule for the parallelization 
    pass in the FuseFlow compiler"""
    path: str'''

[Type.VecStreamLevelChoice]
params.stream_level = "Int"
info.title = "VecStreamLevelChoice"
info.description = """The goal of this step is to choose a stream level for parallelization
when also selecting vectorization parameters."""
info.code = '''@dataclass
class VecStreamLevelChoice:
    """VecStreamLevelChoice

    The goal of this step is to choose a stream level for parallelization
    when also selecting vectorization parameters."""
    path: str

    stream_level: int'''

[Type.VecParFactorChoice]
params.par_factor = "Int"
info.title = "VecParFactorChoice"
info.description = """The goal of this step is to choose a parallelization factor when also
selecting vectorization parameters."""
info.code = '''@dataclass
class VecParFactorChoice:
    """VecParFactorChoice

    The goal of this step is to choose a parallelization factor when also
    selecting vectorization parameters."""
    path: str

    par_factor: int'''

[Type.VecStreamShapeChoice]
params.stream_shape = "Int"
info.title = "VecStreamShapeChoice"
info.description = """The goal of this step is to choose a stream shape when also selecting
vectorization parameters."""
info.code = '''@dataclass
class VecStreamShapeChoice:
    """VecStreamShapeChoice

    The goal of this step is to choose a stream shape when also selecting
    vectorization parameters."""
    path: str

    stream_shape: int'''

[Type.VecBlockSparseChoice]
params.block_sparse = "Bool"
info.title = "VecBlockSparseChoice"
info.description = """The goal of this step is to choose whether block-sparse vectorization is
enabled when also selecting vectorization parameters."""
info.code = '''@dataclass
class VecBlockSparseChoice:
    """VecBlockSparseChoice

    The goal of this step is to choose whether block-sparse vectorization is
    enabled when also selecting vectorization parameters."""
    path: str

    block_sparse: bool'''

[Type.StreamShapeChoice]
params.stream_shape = "Int"
info.title = "StreamShapeChoice"
info.description = """The goal of this step is to choose a stream shape for vectorization."""
info.code = '''@dataclass
class StreamShapeChoice:
    """StreamShapeChoice

    The goal of this step is to choose a stream shape for vectorization."""
    path: str

    stream_shape: int'''

[Type.BlockSparseChoice]
params.block_sparse = "Bool"
info.title = "BlockSparseChoice"
info.description = """The goal of this step is to choose whether block-sparse vectorization is enabled."""
info.code = '''@dataclass
class BlockSparseChoice:
    """BlockSparseChoice

    The goal of this step is to choose whether block-sparse vectorization is enabled."""
    path: str

    block_sparse: bool'''

[Type.StreamLevelChoice]
params.stream_level = "Int"
info.title = "StreamLevelChoice"
info.description = """The goal of this step is to choose a stream level for parallelization."""
info.code = '''@dataclass
class StreamLevelChoice:
    """StreamLevelChoice

    The goal of this step is to choose a stream level for parallelization."""
    path: str

    stream_level: int'''

[Type.ParallelizationPass]
params = {}
info.title = "ParallelizationPass"
info.description = """The goal of this step is to produce the schedule for the parallelization
pass in the FuseFlow compiler"""
info.code = '''@dataclass
class ParallelizationPass:
    """ParallelizationPass

    The goal of this step is to produce the schedule for the parallelization 
    pass in the FuseFlow compiler"""
    path: str'''

[Type.ParFactorChoice]
params.par_factor = "Int"
info.title = "ParFactorChoice"
info.description = """The goal of this step is to choose a parallelization factor."""
info.code = '''@dataclass
class ParFactorChoice:
    """ParFactorChoice

    The goal of this step is to choose a parallelization factor."""
    path: str

    par_factor: int'''

[Type.FuseFlowSchedule]
params = {}
info.title = "FuseFlowSchedule "
info.description = """The goal of this step is to produce a schedule for the FuseFlow compiler
that sets the parallelization and vectorization pass parameters"""
info.code = '''@dataclass
class FuseFlowSchedule:
    """FuseFlowSchedule 

    The goal of this step is to produce a schedule for the FuseFlow compiler 
    that sets the parallelization and vectorization pass parameters"""
    path: str'''

[Function.default_schedule]
params = {}
ret = "FuseFlowSchedule"
condition = [
]
info.title = "schedule "
info.description = """The function that produces a schedule."""
info.hyperparameters = [
]
info.code = '''print("This is a default schedule.")'''

[Function.build_schedule]
params.pass = "ParallelizationPass"
ret = "FuseFlowSchedule"
condition = [
]
info.hyperparameters = [
]
info.code = '''__hb_pass: ParallelizationPass,
__hb_ret: FuseFlowSchedule,

print("Build the schedule")'''

[Function.choose_default_stream_level]
params.par = "ParFactorChoice"
ret = "StreamLevelChoice"
condition = [
    "ret.stream_level = 0",
]
info.hyperparameters = [
]
info.code = '''__hb_par: ParFactorChoice,
__hb_ret: StreamLevelChoice,

print("Choose default stream level (0).")'''

[Function.choose_stream_level_1]
params.par = "ParFactorChoice"
ret = "StreamLevelChoice"
condition = [
    "ret.stream_level = 1",
]
info.hyperparameters = [
]
info.code = '''print("Choose stream level 1.")'''

[Function.choose_stream_level_2]
params.par = "ParFactorChoice"
ret = "StreamLevelChoice"
condition = [
    "ret.stream_level = 2",
]
info.hyperparameters = [
]
info.code = '''print("Choose stream level 2.")'''

[Function.choose_stream_level_4]
params.par = "ParFactorChoice"
ret = "StreamLevelChoice"
condition = [
    "ret.stream_level = 4",
]
info.hyperparameters = [
]
info.code = '''print("Choose stream level 4.")'''

[Function.choose_stream_level_8]
params.par = "ParFactorChoice"
ret = "StreamLevelChoice"
condition = [
    "ret.stream_level = 8",
]
info.hyperparameters = [
]
info.code = '''print("Choose stream level 8.")'''

[Function.choose_stream_level_16]
params.par = "ParFactorChoice"
ret = "StreamLevelChoice"
condition = [
    "ret.stream_level = 16",
]
info.hyperparameters = [
]
info.code = '''print("Choose stream level 16.")'''

[Function.choose_default_par_factor]
params = {}
ret = "ParFactorChoice"
condition = [
    "ret.par_factor = 1",
]
info.hyperparameters = [
]
info.code = '''print("Choose default par factor (1).")'''

[Function.choose_par_factor_2]
params = {}
ret = "ParFactorChoice"
condition = [
    "ret.par_factor = 2",
]
info.hyperparameters = [
]
info.code = '''print("Choose par factor 2.")'''

[Function.choose_par_factor_4]
params = {}
ret = "ParFactorChoice"
condition = [
    "ret.par_factor = 4",
]
info.hyperparameters = [
]
info.code = '''print("Choose par factor 4.")'''

[Function.choose_par_factor_8]
params = {}
ret = "ParFactorChoice"
condition = [
    "ret.par_factor = 8",
]
info.hyperparameters = [
]
info.code = '''print("Choose par factor 8.")'''

[Function.choose_par_factor_16]
params = {}
ret = "ParFactorChoice"
condition = [
    "ret.par_factor = 16",
]
info.hyperparameters = [
]
info.code = '''print("Choose par factor 16.")'''

[Function.choose_default_par_stream_level]
params.par = "VecParFactorChoice"
ret = "VecStreamLevelChoice"
condition = [
    "ret.stream_level = 0",
]
info.hyperparameters = [
]
info.code = '''__hb_par: VecParFactorChoice,
__hb_ret: VecStreamLevelChoice,

print("Choose default stream level (0).")'''

[Function.choose_par_stream_level_1]
params.par = "VecParFactorChoice"
ret = "VecStreamLevelChoice"
condition = [
    "ret.stream_level = 1",
]
info.hyperparameters = [
]
info.code = '''__hb_par: VecParFactorChoice,
__hb_ret: VecStreamLevelChoice,

print("Choose stream level 1.")'''

[Function.choose_par_stream_level_2]
params.par = "VecParFactorChoice"
ret = "VecStreamLevelChoice"
condition = [
    "ret.stream_level = 2",
]
info.hyperparameters = [
]
info.code = '''__hb_par: VecParFactorChoice,
__hb_ret: VecStreamLevelChoice,

print("Choose stream level 2.")'''

[Function.choose_par_stream_level_4]
params.par = "VecParFactorChoice"
ret = "VecStreamLevelChoice"
condition = [
    "ret.stream_level = 4",
]
info.hyperparameters = [
]
info.code = '''__hb_par: VecParFactorChoice,
__hb_ret: VecStreamLevelChoice,

print("Choose stream level 4.")'''

[Function.choose_par_stream_level_8]
params.par = "VecParFactorChoice"
ret = "VecStreamLevelChoice"
condition = [
    "ret.stream_level = 8",
]
info.hyperparameters = [
]
info.code = '''__hb_par: VecParFactorChoice,
__hb_ret: VecStreamLevelChoice,

print("Choose stream level 8.")'''

[Function.choose_par_stream_level_16]
params.par = "VecParFactorChoice"
ret = "VecStreamLevelChoice"
condition = [
    "ret.stream_level = 16",
]
info.hyperparameters = [
]
info.code = '''__hb_par: VecParFactorChoice,
__hb_ret: VecStreamLevelChoice,

print("Choose stream level 16.")'''

[Function.choose_default_par_factor_for_vec]
params.shape = "VecStreamShapeChoice"
ret = "VecParFactorChoice"
condition = [
    "ret.par_factor = 1",
]
info.hyperparameters = [
]
info.code = '''__hb_shape: VecStreamShapeChoice,
__hb_ret: VecParFactorChoice,

print("Choose default par factor (1).")'''

[Function.choose_par_factor_for_vec_2]
params.shape = "VecStreamShapeChoice"
ret = "VecParFactorChoice"
condition = [
    "ret.par_factor = 2",
]
info.hyperparameters = [
]
info.code = '''__hb_shape: VecStreamShapeChoice,
__hb_ret: VecParFactorChoice,

print("Choose par factor 2.")'''

[Function.choose_par_factor_for_vec_4]
params.shape = "VecStreamShapeChoice"
ret = "VecParFactorChoice"
condition = [
    "ret.par_factor = 4",
]
info.hyperparameters = [
]
info.code = '''__hb_shape: VecStreamShapeChoice,
__hb_ret: VecParFactorChoice,

print("Choose par factor 4.")'''

[Function.choose_par_factor_for_vec_8]
params.shape = "VecStreamShapeChoice"
ret = "VecParFactorChoice"
condition = [
    "ret.par_factor = 8",
]
info.hyperparameters = [
]
info.code = '''__hb_shape: VecStreamShapeChoice,
__hb_ret: VecParFactorChoice,

print("Choose par factor 8.")'''

[Function.choose_par_factor_for_vec_16]
params.shape = "VecStreamShapeChoice"
ret = "VecParFactorChoice"
condition = [
    "ret.par_factor = 16",
]
info.hyperparameters = [
]
info.code = '''__hb_shape: VecStreamShapeChoice,
__hb_ret: VecParFactorChoice,

print("Choose par factor 16.")'''

[Function.choose_default_vec_stream_shape]
params.block = "VecBlockSparseChoice"
ret = "VecStreamShapeChoice"
condition = [
    "ret.stream_shape = 16",
]
info.hyperparameters = [
]
info.code = '''__hb_block: VecBlockSparseChoice,
__hb_ret: VecStreamShapeChoice,

print("Choose default stream shape (16).")'''

[Function.choose_vec_stream_shape_1]
params.block = "VecBlockSparseChoice"
ret = "VecStreamShapeChoice"
condition = [
    "ret.stream_shape = 1",
]
info.hyperparameters = [
]
info.code = '''__hb_block: VecBlockSparseChoice,
__hb_ret: VecStreamShapeChoice,

print("Choose stream shape 1.")'''

[Function.choose_vec_stream_shape_2]
params.block = "VecBlockSparseChoice"
ret = "VecStreamShapeChoice"
condition = [
    "ret.stream_shape = 2",
]
info.hyperparameters = [
]
info.code = '''__hb_block: VecBlockSparseChoice,
__hb_ret: VecStreamShapeChoice,

print("Choose stream shape 2.")'''

[Function.choose_vec_stream_shape_4]
params.block = "VecBlockSparseChoice"
ret = "VecStreamShapeChoice"
condition = [
    "ret.stream_shape = 4",
]
info.hyperparameters = [
]
info.code = '''__hb_block: VecBlockSparseChoice,
__hb_ret: VecStreamShapeChoice,

print("Choose stream shape 4.")'''

[Function.choose_vec_stream_shape_8]
params.block = "VecBlockSparseChoice"
ret = "VecStreamShapeChoice"
condition = [
    "ret.stream_shape = 8",
]
info.hyperparameters = [
]
info.code = '''__hb_block: VecBlockSparseChoice,
__hb_ret: VecStreamShapeChoice,

print("Choose stream shape 8.")'''

[Function.choose_vec_stream_shape_16]
params.block = "VecBlockSparseChoice"
ret = "VecStreamShapeChoice"
condition = [
    "ret.stream_shape = 16",
]
info.hyperparameters = [
]
info.code = '''__hb_block: VecBlockSparseChoice,
__hb_ret: VecStreamShapeChoice,

print("Choose stream shape 16.")'''

[Function.choose_default_vec_block_sparse]
params = {}
ret = "VecBlockSparseChoice"
condition = [
    "ret.block_sparse = false",
]
info.hyperparameters = [
]
info.code = '''print("Choose default block sparse (false).")'''

[Function.choose_vec_block_sparse_true]
params = {}
ret = "VecBlockSparseChoice"
condition = [
    "ret.block_sparse = true",
]
info.hyperparameters = [
]
info.code = '''print("Choose block sparse (true).")'''

[Function.choose_default_stream_shape]
params.block = "BlockSparseChoice"
ret = "StreamShapeChoice"
condition = [
    "ret.stream_shape = 16",
]
info.hyperparameters = [
]
info.code = '''__hb_block: BlockSparseChoice,
__hb_ret: StreamShapeChoice,

print("Choose default stream shape (16).")'''

[Function.choose_stream_shape_1]
params.block = "BlockSparseChoice"
ret = "StreamShapeChoice"
condition = [
    "ret.stream_shape = 1",
]
info.hyperparameters = [
]
info.code = '''__hb_block: BlockSparseChoice,
__hb_ret: StreamShapeChoice,

print("Choose stream shape 1.")'''

[Function.choose_stream_shape_2]
params.block = "BlockSparseChoice"
ret = "StreamShapeChoice"
condition = [
    "ret.stream_shape = 2",
]
info.hyperparameters = [
]
info.code = '''__hb_block: BlockSparseChoice,
__hb_ret: StreamShapeChoice,

print("Choose stream shape 2.")'''

[Function.choose_stream_shape_4]
params.block = "BlockSparseChoice"
ret = "StreamShapeChoice"
condition = [
    "ret.stream_shape = 4",
]
info.hyperparameters = [
]
info.code = '''__hb_block: BlockSparseChoice,
__hb_ret: StreamShapeChoice,

print("Choose stream shape 4.")'''

[Function.choose_stream_shape_8]
params.block = "BlockSparseChoice"
ret = "StreamShapeChoice"
condition = [
    "ret.stream_shape = 8",
]
info.hyperparameters = [
]
info.code = '''__hb_block: BlockSparseChoice,
__hb_ret: StreamShapeChoice,

print("Choose stream shape 8.")'''

[Function.choose_stream_shape_16]
params.block = "BlockSparseChoice"
ret = "StreamShapeChoice"
condition = [
    "ret.stream_shape = 16",
]
info.hyperparameters = [
]
info.code = '''__hb_block: BlockSparseChoice,
__hb_ret: StreamShapeChoice,

print("Choose stream shape 16.")'''

[Function.choose_default_block_sparse]
params = {}
ret = "BlockSparseChoice"
condition = [
    "ret.block_sparse = false",
]
info.hyperparameters = [
]
info.code = '''print("Choose default block sparse (false).")'''

[Function.choose_block_sparse_true]
params = {}
ret = "BlockSparseChoice"
condition = [
    "ret.block_sparse = true",
]
info.hyperparameters = [
]
info.code = '''print("Choose block sparse (true).")'''

[Function.parallelization_only]
params.level = "StreamLevelChoice"
ret = "ParallelizationPass"
condition = [
]
info.hyperparameters = [
]
info.code = '''__hb_level: StreamLevelChoice,
__hb_ret: ParallelizationPass,

print("Parallelization only.")'''

[Function.parallelization_and_vectorization]
params.level = "VecStreamLevelChoice"
ret = "ParallelizationPass"
condition = [
]
info.hyperparameters = [
]
info.code = '''__hb_level: VecStreamLevelChoice,
__hb_ret: ParallelizationPass,

print("Parallelization and Vectorization.")'''

[Function.vectorization]
params.shape = "StreamShapeChoice"
ret = "VectorizationPass"
condition = [
]
info.hyperparameters = [
]
info.code = '''print("Vectorization.")'''

